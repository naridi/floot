<% if !user_signed_in? # if the user is not signed in, render this %>
    <style>
        body {
            background: linear-gradient( /* somewhat transparent film on top of the BG to make it look darker. */
                rgba(0,0,0, 0.4),
                rgba(0,0,0, 0.4)
                ), url("/background.png") no-repeat center center fixed
            
            ; /* only display once, center horizontal and vertical, dont let scroll change position. */
            -webkit-background-size: cover;
            -moz-background-size: cover; /* cover the whole html element. */
            -o-background-size: cover;
            background-size: cover;
        }
        h1 {
            color: white;
            font-weight: bold;
        }
        .container-index {
            margin-right: 15%;
            margin-left: 15%;
        }
        .text {
            color: white;
            font-weight: 300;
            font-size: 150%;
        }
        .landing-buttons {
            float: center;
            margin-top: 7.5%;
        }
        .log-in-form {
            margin: 5px;
        }
        .footer-links {
            text-align: center;
            color: white;
            margin-top: 30%;
            font-size: 75%;
        }
    </style>
    
    <div class="container-index row">
        <div class="col-sm-12">
            <h1>Welcome to Floot.</h1>
            <p class="text">
                Your 24 hour disappearing public feed.<br>
            </p>
        </div>
        <div class="col-sm-12">
                   <div class="landing-buttons">
                       <p>
                           <button type="button" class="btn btn-default">Reserve my username.</button>
                      </p>
                    </div> 
        </div>

        <div class="col-sm-12">
            <p class="footer-links">About Terms  Privacy Contact © 2016 Floot, Inc.</p>
        </div>
        
    </div>

<% else # if the user IS signed in, render the following %>

    <script>document.location = "/home"</script>

<% end %>